# Arquitectura del Sistema de Pagos

## Diagrama de Flujo Completo

```
┌──────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                  │
│  1. Usuario completa checkout                                     │
│  2. Click en "Pagar"                                             │
└─────────────────────────┬────────────────────────────────────────┘
                          │
                          │ POST /payments/create/{order_id}
                          │ Headers: Authorization: Bearer {jwt}
                          ▼
┌──────────────────────────────────────────────────────────────────┐
│                      BACKEND (FastAPI)                            │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ routes/payments.py                                       │   │
│  │  - Valida orden                                          │   │
│  │  - Verifica usuario                                      │   │
│  │  - Prepara datos de pago                                │   │
│  └────────────────────────┬────────────────────────────────┘   │
│                           │                                      │
│                           │ payment_service.provider             │
│                           ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ core/payment_service.py (Factory)                        │   │
│  │  - Selecciona proveedor según .env                       │   │
│  │  - Inicializa SDK correspondiente                        │   │
│  └────────────────────────┬────────────────────────────────┘   │
│                           │                                      │
│              ┌────────────┴────────────┐                        │
│              │                         │                        │
│              ▼                         ▼                        │
│  ┌─────────────────────┐   ┌─────────────────────┐            │
│  │ MercadoPago         │   │ Stripe              │            │
│  │ Provider            │   │ Provider            │            │
│  │  - create_payment() │   │  (Futuro)           │            │
│  │  - get_status()     │   │                     │            │
│  │  - refund()         │   │                     │            │
│  └──────────┬──────────┘   └─────────────────────┘            │
│             │                                                   │
└─────────────┼───────────────────────────────────────────────────┘
              │
              │ SDK de MercadoPago
              │ mercadopago.SDK(access_token)
              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    MERCADOPAGO API                                │
│  - Crea preferencia de pago                                      │
│  - Genera checkout_url                                           │
│  - Devuelve payment_id                                           │
└─────────────────────────┬────────────────────────────────────────┘
                          │
                          │ Response: { checkout_url, payment_id }
                          ▼
┌──────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                  │
│  3. Redirige usuario a checkout_url                              │
│     → Usuario paga en MercadoPago                                │
└─────────────────────────┬────────────────────────────────────────┘
                          │
                          │ Usuario completa pago
                          ▼
┌──────────────────────────────────────────────────────────────────┐
│                    MERCADOPAGO                                    │
│  4. Procesa el pago                                              │
│  5. Envía webhook al backend                                     │
└─────────────────────────┬────────────────────────────────────────┘
                          │
                          │ POST /payments/webhook/mercadopago
                          │ Body: { type: "payment", data: {...} }
                          ▼
┌──────────────────────────────────────────────────────────────────┐
│                      BACKEND (FastAPI)                            │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ routes/payments.py - webhook handler                     │   │
│  │  1. Recibe notificación                                  │   │
│  │  2. Consulta estado del pago                             │   │
│  │  3. Actualiza orden en DB                                │   │
│  │     - approved → status = "paid"                         │   │
│  │     - rejected → status = "payment_failed"               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ models/order.py                                          │   │
│  │  order.payment_status = "approved"                       │   │
│  │  order.status = "paid"                                   │   │
│  │  db.commit()                                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
└───────────────────────────┬──────────────────────────────────────┘
                            │
                            │ 6. MercadoPago redirige usuario
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                  │
│  /orders/{order_id}/success                                      │
│  → Muestra confirmación de pago                                  │
│  → Obtiene detalles de la orden                                  │
└──────────────────────────────────────────────────────────────────┘
```

## Arquitectura de Clases

```
┌───────────────────────────────────────────────────────────────────┐
│                     PaymentProvider (ABC)                          │
│  - initialize()                                                    │
│  - create_payment(amount, currency, description, ...)             │
│  - get_payment_status(payment_id)                                 │
│  - cancel_payment(payment_id)                                     │
│  - refund_payment(payment_id, amount)                             │
│  - validate_webhook(payload, signature, secret)                   │
└───────────────────────────┬───────────────────────────────────────┘
                            │ implements
                ┌───────────┴───────────┐
                │                       │
┌───────────────▼──────────┐  ┌────────▼──────────────┐
│  MercadoPagoProvider     │  │   StripeProvider      │
│                          │  │                       │
│  - sdk: mercadopago.SDK  │  │   - stripe: stripe    │
│  - access_token: str     │  │   - api_key: str      │
│  - environment: str      │  │                       │
│                          │  │   (Futuro)            │
│  + initialize()          │  │                       │
│  + create_payment()      │  │                       │
│  + get_payment_status()  │  │                       │
│  + cancel_payment()      │  │                       │
│  + refund_payment()      │  │                       │
│  + validate_webhook()    │  │                       │
│  + get_preference_status()│  │                       │
└──────────────────────────┘  └───────────────────────┘

┌───────────────────────────────────────────────────────────────────┐
│                      PaymentService                                │
│                     (Singleton Factory)                            │
│                                                                    │
│  - _provider: PaymentProvider                                     │
│                                                                    │
│  + initialize(provider_name: str, **config)                       │
│  + provider: PaymentProvider (property)                           │
│                                                                    │
│  Lógica:                                                          │
│    if provider_name == "mercadopago":                             │
│        self._provider = MercadoPagoProvider(...)                  │
│    elif provider_name == "stripe":                                │
│        self._provider = StripeProvider(...)                       │
└───────────────────────────────────────────────────────────────────┘
```

## Flujo de Datos

### 1. Crear Pago

```
Request:
  POST /payments/create/{order_id}
  Headers: Authorization: Bearer {jwt}

Backend Processing:
  1. Validar JWT → Obtener current_user
  2. Buscar Order en DB
  3. Verificar order.user_id == current_user.id
  4. Preparar datos:
     - amount: order.total_amount
     - description: "Orden #12345 - Cisnatura"
     - order_id: order.id
     - customer_email: current_user.email
  5. Llamar payment_service.provider.create_payment()
  6. Guardar payment_id en order
  7. Actualizar order.payment_status = "pending"

Response:
  {
    "success": true,
    "data": {
      "checkout_url": "https://mercadopago.com/...",
      "payment_id": "123456789",
      "amount": 150.00,
      "currency": "MXN"
    }
  }
```

### 2. Webhook de Notificación

```
MercadoPago → Backend:
  POST /payments/webhook/mercadopago
  Headers:
    x-signature: "abc123..."
    x-request-id: "123-456-789"
  Body:
    {
      "type": "payment",
      "data": {
        "id": "123456789"
      }
    }

Backend Processing:
  1. Validar firma (opcional pero recomendado)
  2. Extraer payment_id del body
  3. Consultar estado: get_payment_status(payment_id)
  4. Buscar orden asociada por external_reference
  5. Actualizar orden según status:
     - "approved" → order.status = "paid"
     - "rejected" → order.status = "payment_failed"
     - "pending" → order.status = "pending_payment"
  6. Commit cambios en DB

Response:
  { "success": true }
```

## Estados de Pago (MercadoPago)

```
┌─────────────┐
│   created   │  Pago creado (preferencia)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   pending   │  Usuario en checkout, no ha pagado
└──────┬──────┘
       │
       ├────────────────┬────────────────┐
       │                │                │
       ▼                ▼                ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  approved   │  │  rejected   │  │ in_process  │
│   (paid)    │  │  (failed)   │  │ (reviewing) │
└─────────────┘  └─────────────┘  └──────┬──────┘
       │                                  │
       │                                  ▼
       │                           ┌─────────────┐
       │                           │  approved/  │
       │                           │  rejected   │
       │                           └─────────────┘
       │
       ▼
┌─────────────┐
│  refunded   │  Reembolsado
└─────────────┘
```

## Configuración por Entorno

### Development (.env.local)

```bash
PAYMENT_PROVIDER=mercadopago
MERCADOPAGO_ENVIRONMENT=test
MERCADOPAGO_ACCESS_TOKEN_TEST=TEST-1234...
```

### Production (.env.production)

```bash
PAYMENT_PROVIDER=mercadopago
MERCADOPAGO_ENVIRONMENT=production
MERCADOPAGO_ACCESS_TOKEN_PROD=APP_USR-1234...
```

## Seguridad

```
┌──────────────────────────────────────────────────────────┐
│                    Medidas de Seguridad                   │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  1. JWT Authentication                                    │
│     ✓ Todos los endpoints requieren token válido         │
│                                                           │
│  2. Validación de Webhook                                │
│     ✓ Verificar firma HMAC-SHA256                        │
│     ✓ Validar x-signature header                         │
│                                                           │
│  3. HTTPS en Producción                                  │
│     ✓ TLS 1.2+ obligatorio                               │
│     ✓ Certificado SSL válido                             │
│                                                           │
│  4. Variables de Entorno                                 │
│     ✓ Credenciales en .env (no en código)                │
│     ✓ .env en .gitignore                                 │
│                                                           │
│  5. Validación de Orden                                  │
│     ✓ Verificar order.user_id == current_user.id         │
│     ✓ Verificar order.payment_status != "paid"           │
│                                                           │
│  6. Admin-only Endpoints                                 │
│     ✓ cancel_payment() requiere is_admin                 │
│     ✓ refund_payment() requiere is_admin                 │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## Extensibilidad

Para agregar un nuevo proveedor (ej: PayPal):

```python
# 1. Crear implementación
# app/core/payment_providers/paypal.py
class PayPalProvider(PaymentProvider):
    def initialize(self):
        # Inicializar SDK de PayPal
        pass
    
    def create_payment(self, ...):
        # Crear pago en PayPal
        pass
    
    # ... implementar otros métodos

# 2. Agregar al factory
# app/core/payment_service.py
def initialize(self, provider_name: str, **config):
    if provider_name == "paypal":
        from core.payment_providers.paypal import PayPalProvider
        self._provider = PayPalProvider(**config)
    # ...

# 3. Configurar
# .env
PAYMENT_PROVIDER=paypal
PAYPAL_CLIENT_ID=...
PAYPAL_SECRET=...
```

**¡Todo el resto del código funciona sin cambios!** 🎉
